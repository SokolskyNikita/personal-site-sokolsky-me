---
export const prerender = true;

const pageTitle = "STD risk QALY calculator";
const pageDescription =
  "Private calculator comparing STI and pregnancy risk with and without condoms in a low-risk heterosexual U.S. partner pool.";
const themeColorLight = "#f7f2ea";
const themeColorDark = "#171310";
const siteUrl =
  (Astro.site?.toString() ?? "https://sokolsky.me").replace(/\/$/, "");
const canonicalUrl = `${siteUrl}${Astro.url.pathname}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />
    <meta name="robots" content="noindex, nofollow, noarchive" />
    <meta name="googlebot" content="noindex, nofollow, noarchive" />
    <meta name="referrer" content="strict-origin-when-cross-origin" />
    <meta name="theme-color" content={themeColorLight} />
    <meta name="theme-color" content={themeColorLight} media="(prefers-color-scheme: light)" />
    <meta name="theme-color" content={themeColorDark} media="(prefers-color-scheme: dark)" />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  </head>
  <body>
    <main class="page">
      <header class="hero">
        <a class="home-link" href="/">sokolsky.me/private</a>
        <div>
          <p class="eyebrow">Private decision model</p>
          <h1>STD risk QALY calculator</h1>
          <p class="summary">
            Ten-year estimate for a cishet white U.S. person having only oral and vaginal sex with cishet white opposite-gender partners. No anal sex. No injection-drug-use exposure. The math uses partner prevalence, route-specific transmission, contraceptive failure rates and sex-specific QALY loss.
          </p>
        </div>
      </header>

      <section class="calculator" aria-label="Risk calculator">
        <form class="controls" id="risk-controls">
          <fieldset class="control control-perspective">
            <legend>Perspective</legend>
            <div class="segmented" role="radiogroup" aria-label="Perspective">
              <label><input type="radio" name="perspective" value="female" checked /><span>Female</span></label>
              <label><input type="radio" name="perspective" value="male" /><span>Male</span></label>
            </div>
          </fieldset>

          <div class="control control-partners">
            <div class="label-row">
              <label for="partners-range">Partners over 10 years</label>
              <input id="partners-number" name="partners-number" type="number" min="1" max="200" step="1" value="10" inputmode="numeric" />
            </div>
            <input id="partners-range" name="partners-range" type="range" min="1" max="200" step="1" value="10" />
            <p class="hint">
              Total sex frequency stays fixed at 3 times/week. More partners means fewer acts per partner.
            </p>
          </div>

          <label class="control checkbox">
            <input id="college-only" name="college-only" type="checkbox" checked />
            <span>
              <strong>Only partners with a college degree</strong>
              <small>Unchecking raises HIV and bacterial STI background rates. HSV-2 moves a little too.</small>
            </span>
          </label>

          <label class="control select-control" for="birth-control">
            <span>Birth control when no condoms are used</span>
            <select id="birth-control" name="birth-control">
              <option value="none">None</option>
              <option value="pullout" selected>Perfect pull out</option>
              <option value="pill">Birth control pill</option>
              <option value="implant">Contraceptive implant</option>
            </select>
          </label>
        </form>

        <section class="results" aria-live="polite" aria-label="Calculated results">
          <div class="headline">
            <div>
              <p class="kicker">QALYs saved by using condoms</p>
              <p class="headline-value" id="condom-benefit">-</p>
              <p class="headline-note" id="benefit-note">Waiting for inputs</p>
            </div>
            <div class="mini-facts">
              <span id="acts-per-partner">- acts/partner</span>
              <span>10 years</span>
              <span>3 sex acts/week</span>
            </div>
          </div>

          <div class="comparison-grid">
            <div class="comparison-row">
              <h3>Total lifetime healthy weeks lost</h3>
              <div class="comparison-value without">
                <span>Without condoms</span>
                <strong id="no-total">-</strong>
              </div>
              <div class="comparison-value with">
                <span>With condoms</span>
                <strong id="condom-total">-</strong>
              </div>
            </div>
            <div class="comparison-row benefit-row">
              <h3>QALYs saved by using condoms</h3>
              <div class="comparison-value benefit">
                <span>Compared with no condoms</span>
                <strong id="condom-saved">-</strong>
              </div>
            </div>
            <div class="comparison-row benefit-row">
              <h3>Vaccines can reduce QALY impact by</h3>
              <div class="comparison-value benefit">
                <span>HPV-preventable burden</span>
                <strong id="vaccine-benefit">-</strong>
              </div>
            </div>
            <div class="comparison-row">
              <h3>Odds of at least 1 pregnancy</h3>
              <div class="comparison-value without">
                <span>Without condoms</span>
                <strong id="no-preg-risk">-</strong>
              </div>
              <div class="comparison-value with">
                <span>With condoms</span>
                <strong id="condom-preg-risk">-</strong>
              </div>
            </div>
            <div class="comparison-row">
              <h3>Projected children</h3>
              <div class="comparison-value without">
                <span>Without condoms</span>
                <strong id="no-projected-children">-</strong>
              </div>
              <div class="comparison-value with">
                <span>With condoms</span>
                <strong id="condom-projected-children">-</strong>
              </div>
            </div>
          </div>
        </section>
      </section>

      <section class="detail-grid">
        <article class="panel">
          <div class="section-heading">
            <p class="eyebrow">STI breakdown</p>
            <h2>Lifetime healthy weeks lost by disease</h2>
          </div>
          <div class="breakdown-head" aria-hidden="true">
            <span>Disease</span>
            <span>Without condoms</span>
            <span>With condoms</span>
            <span>Benefit</span>
          </div>
          <div class="breakdown-list" id="disease-breakdown"></div>
        </article>

        <aside class="panel assumptions">
          <div class="section-heading">
            <p class="eyebrow">Model notes</p>
            <h2>Assumptions</h2>
          </div>
          <ul>
            <li>Each STI uses partner prevalence, transmission over the exposure window and sex-specific QALY loss per infection. The HPV-attributable oropharyngeal-cancer row is modeled as a lifetime-risk term, not a per-partner compounding event.</li>
            <li>The college-degree checkbox lowers the demographic-sensitive prevalence terms. HPV and HSV move less than chlamydia, gonorrhea, syphilis, trichomoniasis and HIV.</li>
            <li>Pregnancy uses first-year failure rates: none 85%, perfect withdrawal 4%, pill 9%, implant 0.05%, perfect-use male condom 2%. The with-condoms case uses condoms alone, without pull out or other contraception stacked on top.</li>
            <li>The vaccine estimate applies a 90% HPV-vaccine reduction to HPV-attributable rows only; it does not change HSV, bacterial STI, HIV or pregnancy terms.</li>
            <li>Projected children uses expected pregnancies over the same 10 years and assumes 50% of unintended pregnancies result in a child.</li>
            <li>Female pregnancy QALY uses 159 healthy days per unintended pregnancy from Schwarz et al. Male direct-health pregnancy QALY is 0, but pregnancy probability is still shown.</li>
            <li>The shakiest inputs are HPV persistence/progression, HSV transmission heterogeneity and pregnancy utility.</li>
          </ul>
        </aside>
      </section>

      <section class="sources" aria-labelledby="sources-heading">
        <h2 id="sources-heading">Sources and anchors</h2>
        <p>
          This is a calculator, not a clinical forecast. It uses CDC contraception, HIV and HPV vaccination tables, NHANES HPV/HSV prevalence, Li et al. STI QALY estimates, D'Souza et al. oral HPV/OPC risk, Martin/Magaret HSV condom studies, Winer NEJM genital HPV condom data and Schwarz et al. unintended-pregnancy utility.
        </p>
        <ul>
          <li><a href="https://www.cdc.gov/mmwr/preview/mmwrhtml/rr6304a5.htm" target="_blank" rel="noreferrer noopener">CDC contraceptive effectiveness table</a></li>
          <li><a href="https://www.cdc.gov/hivpartners/php/riskandprevention/index.html" target="_blank" rel="noreferrer noopener">CDC HIV risk and prevention estimates</a></li>
          <li><a href="https://www.cdc.gov/sti/about/about-sti-risk-and-oral-sex.html" target="_blank" rel="noreferrer noopener">CDC STI risk and oral sex</a></li>
          <li><a href="https://www.cdc.gov/sti-statistics/annual/index.html" target="_blank" rel="noreferrer noopener">CDC 2024 STI surveillance</a></li>
          <li><a href="https://www.cdc.gov/nchs/products/databriefs/db280.htm" target="_blank" rel="noreferrer noopener">CDC NCHS HPV prevalence data brief</a></li>
          <li><a href="https://www.cdc.gov/hpv/vaccines/index.html" target="_blank" rel="noreferrer noopener">CDC HPV vaccine prevention summary</a></li>
          <li><a href="https://www.cdc.gov/nchs/products/databriefs/db304.htm" target="_blank" rel="noreferrer noopener">CDC NCHS HSV-1/HSV-2 prevalence data brief</a></li>
          <li><a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC10112678/" target="_blank" rel="noreferrer noopener">QALYs lost from chlamydia, gonorrhea and trichomoniasis</a></li>
          <li><a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC5834136/" target="_blank" rel="noreferrer noopener">Oral HPV prevalence and OPC lifetime risk</a></li>
          <li><a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC2580059/" target="_blank" rel="noreferrer noopener">Unintended pregnancy utility / QALY study</a></li>
        </ul>
      </section>
    </main>

    <script is:inline>
      const YEARS = 10;
      const ACTS_PER_WEEK = 3;
      const WEEKS_PER_YEAR = 52;
      const TOTAL_ACTS = YEARS * WEEKS_PER_YEAR * ACTS_PER_WEEK;
      const DAYS_PER_QALY = 365.25;
      const WEEKS_PER_QALY = DAYS_PER_QALY / 7;
      const PREGNANCY_QALY_DAYS = 159;
      const CHILD_PER_PREGNANCY = 0.5;
      const HPV_VACCINE_EFFECTIVENESS = 0.9;

      const DISEASES = [
        {
          name: "HPV-attributable OPC",
          route: "lifetime",
          repeatable: false,
          note: "Approximate lifetime oropharyngeal-cancer burden. Condoms do not change this term; male OPC risk is much higher than female OPC risk.",
          lifetimeRisk: {
            college: { female: 0.0035, male: 0.0095 },
            broad: { female: 0.0045, male: 0.011 }
          },
          condomEffectiveness: { female: 0, male: 0 },
          vaccineEffectiveness: HPV_VACCINE_EFFECTIVENESS,
          qalyPerCase: { female: 0.025, male: 0.041 }
        },
        {
          name: "Genital HPV",
          route: "perPartner",
          repeatable: false,
          note: "Genital high-risk HPV plus wart/dysplasia burden. Condoms reduce skin-contact exposure but do not remove it.",
          perPartner: {
            college: { female: 0.02, male: 0.018 },
            broad: { female: 0.03, male: 0.027 }
          },
          condomEffectiveness: { female: 0.55, male: 0.55 },
          vaccineEffectiveness: HPV_VACCINE_EFFECTIVENESS,
          qalyPerCase: { female: 0.012, male: 0.003 }
        },
        {
          name: "HSV-2",
          route: "perAct",
          repeatable: false,
          note: "HSV-2 prevalence starts from NHANES white HSV-2 rates, then drops for the college-pool setting.",
          prevalence: {
            college: { male: 0.055, female: 0.04 },
            broad: { male: 0.085, female: 0.065 }
          },
          perAct: { female: 0.0011, male: 0.00065 },
          condomEffectiveness: { female: 0.96, male: 0.65 },
          qalyPerCase: { female: 0.035, male: 0.02 }
        },
        {
          name: "HSV-1 genital",
          route: "perPartner",
          repeatable: false,
          note: "Mostly oral-acquired genital HSV-1. Condoms do not change it when oral sex is unbarriered.",
          perPartner: {
            college: { female: 0.0009, male: 0.0007 },
            broad: { female: 0.0012, male: 0.001 }
          },
          condomEffectiveness: { female: 0, male: 0 },
          qalyPerCase: { female: 0.01, male: 0.008 }
        },
        {
          name: "Chlamydia",
          route: "perAct",
          repeatable: true,
          maxExposureActs: 20,
          note: "Female QALY/case uses Li et al.'s PID, chronic pelvic pain, tubal-factor infertility and ectopic-pregnancy model.",
          prevalence: {
            college: { male: 0.004, female: 0.005 },
            broad: { male: 0.012, female: 0.014 }
          },
          perAct: { female: 0.04, male: 0.032 },
          condomEffectiveness: { female: 0.6, male: 0.6 },
          qalyPerCase: { female: 0.04698, male: 0.00094 }
        },
        {
          name: "Gonorrhea",
          route: "perAct",
          repeatable: true,
          maxExposureActs: 12,
          note: "Shorter infectious window than chlamydia. Female sequelae still drive most QALY loss.",
          prevalence: {
            college: { male: 0.0009, female: 0.0008 },
            broad: { male: 0.0035, female: 0.003 }
          },
          perAct: { female: 0.09, male: 0.07 },
          condomEffectiveness: { female: 0.65, male: 0.65 },
          qalyPerCase: { female: 0.01422, male: 0.00143 }
        },
        {
          name: "Trichomoniasis",
          route: "perAct",
          repeatable: true,
          maxExposureActs: 20,
          note: "White-pool prevalence is much lower than overall U.S. prevalence. QALY/case comes from Li et al.",
          prevalence: {
            college: { male: 0.002, female: 0.004 },
            broad: { male: 0.005, female: 0.008 }
          },
          perAct: { female: 0.07, male: 0.055 },
          condomEffectiveness: { female: 0.6, male: 0.6 },
          qalyPerCase: { female: 0.00135, male: 0.00012 }
        },
        {
          name: "Syphilis",
          route: "perAct",
          repeatable: true,
          maxExposureActs: 10,
          note: "Very low in this non-IDU heterosexual pool. QALY/case is high if acquired.",
          prevalence: {
            college: { male: 0.00012, female: 0.00006 },
            broad: { male: 0.0005, female: 0.00018 }
          },
          perAct: { female: 0.02, male: 0.016 },
          condomEffectiveness: { female: 0.5, male: 0.5 },
          qalyPerCase: { female: 0.09, male: 0.09 }
        },
        {
          name: "HIV",
          route: "perAct",
          repeatable: false,
          note: "Uses CDC per-act vaginal risk, filtered to a low-prevalence heterosexual white non-IDU pool.",
          prevalence: {
            college: { male: 0.00004, female: 0.000025 },
            broad: { male: 0.00018, female: 0.0001 }
          },
          perAct: { female: 0.0008, male: 0.0004 },
          condomEffectiveness: { female: 0.8, male: 0.8 },
          qalyPerCase: { female: 6, male: 6 }
        }
      ];

      const BIRTH_CONTROL = {
        none: { label: "None", annualFailure: 0.85 },
        pullout: { label: "Perfect pull out", annualFailure: 0.04 },
        pill: { label: "Birth control pill", annualFailure: 0.09 },
        implant: { label: "Contraceptive implant", annualFailure: 0.0005 }
      };

      const CONDOM_ANNUAL_FAILURE = 0.02;
      const PREGNANCY_QALY_PER_CASE = {
        female: PREGNANCY_QALY_DAYS / DAYS_PER_QALY,
        male: 0
      };

      const form = document.querySelector("#risk-controls");
      const partnersRange = document.querySelector("#partners-range");
      const partnersNumber = document.querySelector("#partners-number");
      const collegeOnly = document.querySelector("#college-only");
      const birthControl = document.querySelector("#birth-control");
      const diseaseBreakdown = document.querySelector("#disease-breakdown");

      const ids = {
        condomBenefit: document.querySelector("#condom-benefit"),
        benefitNote: document.querySelector("#benefit-note"),
        actsPerPartner: document.querySelector("#acts-per-partner"),
        noTotal: document.querySelector("#no-total"),
        noPregRisk: document.querySelector("#no-preg-risk"),
        noProjectedChildren: document.querySelector("#no-projected-children"),
        condomTotal: document.querySelector("#condom-total"),
        condomSaved: document.querySelector("#condom-saved"),
        vaccineBenefit: document.querySelector("#vaccine-benefit"),
        condomPregRisk: document.querySelector("#condom-preg-risk"),
        condomProjectedChildren: document.querySelector("#condom-projected-children")
      };

      function clampPartners(value) {
        const parsed = Number.parseInt(value, 10);
        if (Number.isNaN(parsed)) return 10;
        return Math.min(200, Math.max(1, parsed));
      }

      function probabilityAcrossPartners(perPartnerRisk, partners) {
        return 1 - Math.pow(1 - perPartnerRisk, partners);
      }

      function perActPartnerRisk(disease, perspective, partnerSex, tier, actsPerPartner, withCondom) {
        const infectiousPrevalence = disease.prevalence[tier][partnerSex];
        const effectiveActs = Math.min(actsPerPartner, disease.maxExposureActs ?? actsPerPartner);
        const condomMultiplier = withCondom ? 1 - disease.condomEffectiveness[perspective] : 1;
        const perActRisk = disease.perAct[perspective] * condomMultiplier;
        const transmissionIfInfectious = 1 - Math.pow(1 - perActRisk, effectiveActs);

        return infectiousPrevalence * transmissionIfInfectious;
      }

      function perPartnerRisk(disease, perspective, withCondom, tier) {
        const baseRisk = disease.perPartner[tier][perspective];
        const condomMultiplier = withCondom ? 1 - disease.condomEffectiveness[perspective] : 1;
        return baseRisk * condomMultiplier;
      }

      function lifetimeRisk(disease, perspective, withCondom, tier) {
        const baseRisk = disease.lifetimeRisk[tier][perspective];
        const condomMultiplier = withCondom ? 1 - disease.condomEffectiveness[perspective] : 1;
        return baseRisk * condomMultiplier;
      }

      function diseaseOutcome(disease, context, withCondom) {
        const perPartner = disease.route === "perAct"
          ? perActPartnerRisk(
              disease,
              context.perspective,
              context.partnerSex,
              context.tier,
              context.actsPerPartner,
              withCondom
            )
          : disease.route === "lifetime"
            ? lifetimeRisk(disease, context.perspective, withCondom, context.tier)
            : perPartnerRisk(disease, context.perspective, withCondom, context.tier);
        const probability = disease.route === "lifetime"
          ? perPartner
          : probabilityAcrossPartners(perPartner, context.partners);
        const expectedCases = disease.repeatable ? perPartner * context.partners : probability;
        const qaly = expectedCases * disease.qalyPerCase[context.perspective];

        return { perPartner, probability, expectedCases, qaly };
      }

      function pregnancyFor(annualFailure, perspective) {
        const expectedPregnancies = annualFailure * YEARS;
        const atLeastOne = 1 - Math.pow(1 - annualFailure, YEARS);
        const projectedChildren = expectedPregnancies * CHILD_PER_PREGNANCY;

        return {
          probability: atLeastOne,
          expected: expectedPregnancies,
          projectedChildren,
          qaly: expectedPregnancies * PREGNANCY_QALY_PER_CASE[perspective]
        };
      }

      function weeksText(qaly) {
        const weeks = qaly * WEEKS_PER_QALY;
        if (weeks === 0) return "0 weeks";
        if (weeks < 0.001) return `${weeks.toFixed(5)} weeks`;
        if (weeks < 0.01) return `${weeks.toFixed(4)} weeks`;
        if (weeks < 0.1) return `${weeks.toFixed(3)} weeks`;
        if (weeks < 10) return `${weeks.toFixed(2)} weeks`;
        return `${weeks.toFixed(1)} weeks`;
      }

      function timeText(qaly) {
        return weeksText(qaly);
      }

      function percentText(value) {
        if (value < 0.0001) return `${(value * 100).toFixed(3)}%`;
        if (value < 0.001) return `${(value * 100).toFixed(2)}%`;
        if (value < 0.01) return `${(value * 100).toFixed(1)}%`;
        return `${(value * 100).toFixed(0)}%`;
      }

      function signedTimeText(value) {
        const abs = Math.abs(value);
        const label = timeText(abs);
        if (value > 0) return `${label} saved`;
        if (value < 0) return `${label} worse`;
        return "No modeled benefit";
      }

      function childrenText(value) {
        return value.toFixed(1);
      }

      function calculateDiseaseTotals({ perspective, partners, onlyCollege }) {
        const context = {
          perspective,
          partners,
          partnerSex: perspective === "female" ? "male" : "female",
          tier: onlyCollege ? "college" : "broad",
          actsPerPartner: TOTAL_ACTS / partners
        };

        return DISEASES.map((disease) => {
          const noCondom = diseaseOutcome(disease, context, false);
          const condom = diseaseOutcome(disease, context, true);

          return {
            name: disease.name,
            note: disease.note,
            noRisk: noCondom.probability,
            condomRisk: condom.probability,
            noCases: noCondom.expectedCases,
            condomCases: condom.expectedCases,
            noCondom: noCondom.qaly,
            condom: condom.qaly,
            vaccineBenefit: noCondom.qaly * (disease.vaccineEffectiveness ?? 0),
            benefit: noCondom.qaly - condom.qaly
          };
        });
      }

      function renderBreakdown(rows) {
        diseaseBreakdown.innerHTML = rows
          .map((row) => `
            <article class="breakdown-row">
              <div class="disease-name">
                <strong>${row.name}</strong>
                <span>${row.note}</span>
              </div>
              <div><span class="mobile-label">Without condoms</span> ${weeksText(row.noCondom)}</div>
              <div><span class="mobile-label">With condoms</span> ${weeksText(row.condom)}</div>
              <div><span class="mobile-label">Benefit</span> ${weeksText(row.benefit)}</div>
            </article>
          `)
          .join("");
      }

      function setText(node, text) {
        if (node) node.textContent = text;
      }

      function update() {
        const partners = clampPartners(partnersRange.value);
        const perspective = new FormData(form).get("perspective") || "female";
        const onlyCollege = collegeOnly.checked;
        const birthControlKey = birthControl.value;
        const noCondomBirthControl = BIRTH_CONTROL[birthControlKey] || BIRTH_CONTROL.pullout;

        partnersRange.value = String(partners);
        partnersNumber.value = String(partners);

        const diseaseRows = calculateDiseaseTotals({ perspective, partners, onlyCollege });
        const noCondomSti = diseaseRows.reduce((total, row) => total + row.noCondom, 0);
        const condomSti = diseaseRows.reduce((total, row) => total + row.condom, 0);
        const vaccineBenefit = diseaseRows.reduce((total, row) => total + row.vaccineBenefit, 0);
        const noCondomPregnancy = pregnancyFor(noCondomBirthControl.annualFailure, perspective);
        const condomPregnancy = pregnancyFor(CONDOM_ANNUAL_FAILURE, perspective);
        const noCondomTotal = noCondomSti + noCondomPregnancy.qaly;
        const condomTotal = condomSti + condomPregnancy.qaly;
        const benefit = noCondomTotal - condomTotal;
        const actsPerPartner = TOTAL_ACTS / partners;

        setText(ids.actsPerPartner, `${actsPerPartner.toFixed(actsPerPartner < 10 ? 1 : 0)} acts/partner`);
        setText(ids.condomBenefit, signedTimeText(benefit));
        setText(
          ids.benefitNote,
          benefit >= 0
            ? `Using condoms saves ${weeksText(Math.abs(benefit))} of healthy time over this 10-year exposure window.`
            : `Without condoms is lower here because ${noCondomBirthControl.label.toLowerCase()} prevents pregnancy better than condoms in this model.`
        );

        setText(ids.noTotal, weeksText(noCondomTotal));
        setText(ids.noPregRisk, percentText(noCondomPregnancy.probability));
        setText(ids.noProjectedChildren, childrenText(noCondomPregnancy.projectedChildren));

        setText(ids.condomTotal, weeksText(condomTotal));
        setText(ids.condomSaved, signedTimeText(benefit));
        setText(ids.vaccineBenefit, weeksText(vaccineBenefit));
        setText(ids.condomPregRisk, percentText(condomPregnancy.probability));
        setText(ids.condomProjectedChildren, childrenText(condomPregnancy.projectedChildren));

        renderBreakdown(diseaseRows);
      }

      partnersRange.addEventListener("input", () => {
        partnersNumber.value = partnersRange.value;
        update();
      });

      partnersNumber.addEventListener("input", () => {
        const value = clampPartners(partnersNumber.value);
        partnersRange.value = String(value);
        update();
      });

      form.addEventListener("input", update);
      form.addEventListener("change", update);

      update();
    </script>
  </body>
</html>

<style>
  :root {
    color-scheme: light dark;
    --bg: #f7f2ea;
    --panel: #fffaf2;
    --panel-strong: #f0e3d1;
    --text: #241b14;
    --muted: #77675a;
    --faint: #9b8c7b;
    --line: rgba(72, 48, 28, 0.16);
    --accent: #8b4e24;
    --accent-soft: rgba(139, 78, 36, 0.13);
    --without: #8a3b2d;
    --without-soft: rgba(138, 59, 45, 0.1);
    --with: #286044;
    --with-soft: rgba(40, 96, 68, 0.1);
    --good: #286044;
    --bad: #8a3b2d;
    --shadow: 0 22px 70px rgba(72, 48, 28, 0.1);
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --bg: #171310;
      --panel: #211b16;
      --panel-strong: #2a2119;
      --text: #f4eadc;
      --muted: #b9aa99;
      --faint: #8f8171;
      --line: rgba(255, 235, 208, 0.14);
      --accent: #e3a066;
      --accent-soft: rgba(227, 160, 102, 0.14);
      --without: #e0a092;
      --without-soft: rgba(224, 160, 146, 0.12);
      --with: #8fc6a3;
      --with-soft: rgba(143, 198, 163, 0.12);
      --good: #8fc6a3;
      --bad: #e0a092;
      --shadow: 0 22px 70px rgba(0, 0, 0, 0.28);
    }
  }

  * {
    box-sizing: border-box;
  }

  html {
    min-height: 100%;
    background:
      radial-gradient(circle at 8% 4%, var(--accent-soft), transparent 23rem),
      linear-gradient(135deg, color-mix(in srgb, var(--bg) 92%, var(--panel-strong)), var(--bg));
    color: var(--text);
    font-family:
      ui-sans-serif,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      sans-serif;
    font-size: 15px;
    line-height: 1.45;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    min-height: 100vh;
    margin: 0;
  }

  a {
    color: var(--accent);
    text-decoration-color: color-mix(in srgb, var(--accent) 38%, transparent);
    text-underline-offset: 0.18em;
  }

  a:hover {
    text-decoration-color: currentColor;
  }

  .page {
    width: min(100%, 72rem);
    margin: 0 auto;
    padding: clamp(0.8rem, 2vw, 1.4rem);
  }

  .hero {
    display: grid;
    grid-template-columns: minmax(8rem, 0.28fr) minmax(0, 1fr);
    gap: clamp(0.8rem, 2.8vw, 2rem);
    align-items: start;
    margin-bottom: 0.9rem;
  }

  .home-link {
    color: var(--muted);
    font-size: 0.78rem;
    letter-spacing: 0.02em;
    text-decoration: none;
  }

  .eyebrow,
  .kicker {
    margin: 0 0 0.25rem;
    color: var(--accent);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h1,
  h2 {
    margin: 0;
    font-family:
      ui-serif,
      Georgia,
      Cambria,
      "Times New Roman",
      serif;
    font-weight: 650;
    letter-spacing: -0.025em;
  }

  h1 {
    font-size: clamp(1.55rem, 4vw, 3rem);
    line-height: 0.98;
  }

  h2 {
    font-size: clamp(1.05rem, 1.7vw, 1.35rem);
    line-height: 1.08;
  }

  .summary {
    max-width: 58rem;
    margin: 0.55rem 0 0;
    color: var(--muted);
    font-size: clamp(0.86rem, 1.4vw, 0.98rem);
    text-wrap: pretty;
  }

  .calculator,
  .panel,
  .sources {
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--panel) 92%, transparent);
    box-shadow: var(--shadow);
  }

  .calculator {
    display: grid;
    grid-template-columns: minmax(16rem, 0.86fr) minmax(0, 1.14fr);
    gap: 0;
    overflow: hidden;
    border-radius: 26px;
  }

  .controls,
  .results {
    padding: clamp(0.85rem, 2.2vw, 1.35rem);
  }

  .controls {
    display: grid;
    gap: 0.8rem;
    border-right: 1px solid var(--line);
    background: color-mix(in srgb, var(--panel-strong) 44%, transparent);
  }

  .control {
    min-width: 0;
    margin: 0;
    padding: 0;
    border: 0;
  }

  legend,
  .control > span,
  .label-row label,
  .select-control span {
    display: block;
    margin-bottom: 0.35rem;
    color: var(--text);
    font-size: 0.78rem;
    font-weight: 750;
  }

  .segmented {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.28rem;
    padding: 0.25rem;
    border: 1px solid var(--line);
    border-radius: 999px;
    background: color-mix(in srgb, var(--panel) 70%, transparent);
  }

  .segmented label {
    min-width: 0;
    cursor: pointer;
  }

  .segmented input {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: 0;
    opacity: 0;
    pointer-events: none;
  }

  .segmented span {
    display: block;
    padding: 0.42rem 0.58rem;
    border-radius: 999px;
    color: var(--muted);
    font-size: 0.78rem;
    font-weight: 720;
    text-align: center;
    transition:
      background 160ms ease,
      color 160ms ease;
  }

  .segmented input:checked + span {
    background: var(--text);
    color: var(--bg);
  }

  .label-row {
    display: flex;
    gap: 0.6rem;
    align-items: center;
    justify-content: space-between;
  }

  input,
  select {
    width: 100%;
    min-width: 0;
    color: var(--text);
    font: inherit;
  }

  input[type="number"],
  select {
    border: 1px solid var(--line);
    border-radius: 12px;
    background: var(--panel);
  }

  input[type="number"] {
    max-width: 5.4rem;
    padding: 0.38rem 0.48rem;
    font-size: 0.86rem;
  }

  input[type="range"] {
    accent-color: var(--accent);
  }

  select {
    padding: 0.48rem 0.56rem;
    font-size: 0.86rem;
  }

  .hint,
  small {
    display: block;
    margin: 0.25rem 0 0;
    color: var(--muted);
    font-size: 0.73rem;
    line-height: 1.35;
  }

  .checkbox {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.55rem;
    align-items: start;
    padding: 0.6rem;
    border: 1px solid var(--line);
    border-radius: 16px;
    cursor: pointer;
  }

  .checkbox input {
    width: 1rem;
    height: 1rem;
    margin-top: 0.12rem;
    accent-color: var(--accent);
  }

  .checkbox strong {
    display: block;
    font-size: 0.8rem;
  }

  .headline {
    display: flex;
    gap: 1rem;
    align-items: start;
    justify-content: space-between;
    padding-bottom: 0.85rem;
    border-bottom: 1px solid var(--line);
  }

  .headline-value {
    margin: 0;
    font-family:
      ui-serif,
      Georgia,
      Cambria,
      "Times New Roman",
      serif;
    font-size: clamp(1.65rem, 4vw, 3.1rem);
    font-weight: 680;
    line-height: 0.98;
    letter-spacing: -0.04em;
  }

  .headline-note {
    margin: 0.35rem 0 0;
    color: var(--muted);
    font-size: 0.82rem;
  }

  .mini-facts {
    display: flex;
    max-width: 13rem;
    flex-wrap: wrap;
    gap: 0.35rem;
    justify-content: flex-end;
  }

  .mini-facts span {
    padding: 0.22rem 0.42rem;
    border: 1px solid var(--line);
    border-radius: 999px;
    color: var(--muted);
    font-size: 0.68rem;
    white-space: nowrap;
  }

  .comparison-grid {
    display: grid;
    gap: 0.55rem;
    margin-top: 0.85rem;
  }

  .comparison-row {
    display: grid;
    grid-template-columns: minmax(10rem, 0.84fr) repeat(2, minmax(7.4rem, 0.58fr));
    gap: 0.55rem;
    align-items: stretch;
  }

  .comparison-row.benefit-row {
    grid-template-columns: minmax(10rem, 0.84fr) minmax(0, 1.16fr);
  }

  .comparison-row h3 {
    min-width: 0;
    margin: 0;
    padding: 0.7rem 0.2rem 0.7rem 0;
    align-self: center;
    color: var(--text);
    font-size: 0.84rem;
    font-weight: 820;
    line-height: 1.25;
  }

  .comparison-value {
    min-width: 0;
    padding: 0.7rem 0.72rem;
    border: 1px solid var(--line);
    border-radius: 16px;
    background: color-mix(in srgb, var(--panel) 78%, transparent);
  }

  .comparison-value.without {
    border-color: color-mix(in srgb, var(--without) 34%, var(--line));
    background: var(--without-soft);
  }

  .comparison-value.with {
    border-color: color-mix(in srgb, var(--with) 34%, var(--line));
    background: var(--with-soft);
  }

  .comparison-value.benefit {
    border-color: color-mix(in srgb, var(--accent) 38%, var(--line));
    background:
      linear-gradient(135deg, color-mix(in srgb, var(--accent-soft) 84%, transparent), transparent 72%),
      color-mix(in srgb, var(--panel) 76%, transparent);
  }

  .comparison-value span {
    display: block;
    margin-bottom: 0.2rem;
    font-size: 0.64rem;
    font-weight: 860;
    letter-spacing: 0.08em;
    line-height: 1.15;
    text-transform: uppercase;
  }

  .comparison-value.without span {
    color: var(--without);
  }

  .comparison-value.with span {
    color: var(--with);
  }

  .comparison-value.benefit span {
    color: var(--accent);
  }

  .comparison-value strong {
    display: block;
    color: var(--text);
    font-family:
      ui-serif,
      Georgia,
      Cambria,
      "Times New Roman",
      serif;
    font-size: clamp(1.22rem, 2.2vw, 1.75rem);
    font-weight: 720;
    line-height: 1.02;
    letter-spacing: -0.035em;
  }

  .detail-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.34fr) minmax(16rem, 0.66fr);
    gap: 0.9rem;
    margin-top: 0.9rem;
  }

  .panel,
  .sources {
    border-radius: 24px;
    padding: clamp(0.85rem, 2vw, 1.2rem);
  }

  .section-heading {
    display: flex;
    gap: 0.7rem;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 0.75rem;
  }

  .breakdown-head,
  :global(.breakdown-row) {
    display: grid;
    grid-template-columns: minmax(8rem, 1fr) repeat(3, minmax(7rem, 0.34fr));
    gap: 0.7rem;
    align-items: baseline;
  }

  .breakdown-head {
    padding: 0 0.55rem 0.35rem;
    color: var(--faint);
    font-size: 0.66rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .breakdown-head span:not(:first-child),
  :global(.breakdown-row > div:not(:first-child)) {
    text-align: right;
    white-space: nowrap;
  }

  .breakdown-list {
    display: grid;
    gap: 0.35rem;
  }

  :global(.breakdown-row) {
    min-width: 0;
    padding: 0.55rem;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: color-mix(in srgb, var(--panel) 68%, transparent);
    font-size: 0.78rem;
  }

  :global(.disease-name) {
    min-width: 0;
  }

  :global(.disease-name strong) {
    display: block;
    font-size: 0.8rem;
  }

  :global(.disease-name span) {
    display: block;
    margin-top: 0.1rem;
    color: var(--muted);
    font-size: 0.69rem;
    line-height: 1.3;
    text-wrap: pretty;
  }

  :global(.mobile-label) {
    display: none;
  }

  .assumptions ul,
  .sources ul {
    margin: 0;
    padding-left: 1.05rem;
  }

  .assumptions li,
  .sources li {
    margin: 0.35rem 0;
    color: var(--muted);
    font-size: 0.78rem;
  }

  .sources {
    margin-top: 0.9rem;
  }

  .sources h2 {
    margin-bottom: 0.45rem;
  }

  .sources p {
    margin: 0 0 0.6rem;
    color: var(--muted);
    font-size: 0.8rem;
    text-wrap: pretty;
  }

  @media (max-width: 820px) {
    .page {
      padding: 0.75rem;
    }

    .hero,
    .calculator,
    .detail-grid {
      grid-template-columns: 1fr;
    }

    .hero {
      gap: 0.45rem;
    }

    .controls {
      border-right: 0;
      border-bottom: 1px solid var(--line);
    }

    .headline {
      display: grid;
    }

    .mini-facts {
      max-width: none;
      justify-content: flex-start;
    }
  }

  @media (max-width: 560px) {
    html {
      font-size: 14px;
    }

    .page {
      padding: 0.55rem;
    }

    .calculator,
    .panel,
    .sources {
      border-radius: 18px;
    }

    .comparison-row {
      grid-template-columns: 1fr 1fr;
      gap: 0.42rem;
    }

    .comparison-row.benefit-row {
      grid-template-columns: 1fr;
    }

    .comparison-row h3 {
      grid-column: 1 / -1;
      padding: 0.2rem 0 0;
      font-size: 0.82rem;
    }

    .comparison-value {
      padding: 0.62rem;
    }

    .comparison-value span {
      font-size: 0.58rem;
      letter-spacing: 0.06em;
    }

    .section-heading {
      display: block;
    }

    .breakdown-head {
      display: none;
    }

    :global(.breakdown-row) {
      grid-template-columns: 1fr;
      gap: 0.35rem;
    }

    :global(.breakdown-row > div:not(:first-child)) {
      display: flex;
      justify-content: space-between;
      gap: 1rem;
      text-align: left;
    }

    :global(.mobile-label) {
      display: inline;
      color: var(--muted);
      font-size: 0.7rem;
      font-weight: 700;
    }
  }
</style>
