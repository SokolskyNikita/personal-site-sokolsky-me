# Copy to .dev.vars (gitignored) for local wrangler/astro worker testing.
SERPAPI_API_KEY=your_serpapi_key_here
# Optional: when set, /api/flights/query requires X-Search-Access-Token
# SEARCH_ACCESS_TOKEN=optional_shared_secret
